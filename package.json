{
  "name": "lvqm-website",
  "version": "1.0.0",
  "description": "LVQM website",
  "main": "app.js",
  "scripts": {
    "postinstall": "cd client && npm install",
    "start": "npm run build:ui && node app.js",
    "build:ui": "rm -rf client/build && cd ./client && npm run build",
    "heroku-postbuild": "rm -rf client/build && cd ./client && npm run build --prod"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.20.0",
    "cors": "^2.8.5",
    "dotenv": "^16.0.1",
    "express": "^4.18.1",
    "node-mailjet": "^5.0.0",
    "nodemon": "^2.0.20",
    "react-gtm-module": "^2.0.11",
    "stripe": "^9.10.0"
  }
}
